/** Compile-time start policy, not a URL/storage override. Cold recovery lives
 * separately and MUST remain available when new imports are disabled. */
export const WORKSPACE_SYNC_APPLY_START_ENABLED = false
