node_modules
dist
.env
.env.*
!.env.example
*.local
*.tsbuildinfo

# Claude Code worktrees (local only, never commit)
.claude/worktrees/

# Playwright MCP artifacts (snapshots / console logs / screenshots, local only)
.playwright-mcp/
arty-*.png

# Computer-use relay runtime logs (local tooling, never commit)
local/*.log

# Native build secrets
google-services.json
GoogleService-Info.plist
*.keystore
*.jks
*.p12
local.properties
*.ipa
*.apk
*.aab
