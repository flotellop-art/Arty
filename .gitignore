node_modules
dist
coverage
artifacts/vision-a11/
.env
.env.*
!.env.example
*.local
*.tsbuildinfo

# Claude Code worktrees (local only, never commit)
.claude/worktrees/

# Playwright MCP artifacts (snapshots / console logs, local only)
.playwright-mcp/

# Computer-use relay runtime logs (local tooling, never commit)
local/*.log

# Native build secrets
google-services.json
GoogleService-Info.plist
*.keystore
*.jks
*.p12
local.properties
*.ipa
*.apk
*.aab

# Google Workspace Add-on Phase 0 — rendered deployment artifacts
google-workspace-addon/phase0/deployment.phase0.json
google-workspace-addon/phase0/context-card.phase0.json
google-workspace-addon/phase0/cloudflare.vars.phase0.json
