Arty 1.0.21 — Fix connexion Google qui échoue silencieusement

• Fix : quand tu cliques "Connecter Google" et que le popup s'ouvre mais que rien ne se passe, tu vois maintenant la VRAIE erreur (serverAuthCode manquant, échange de token échoué côté serveur, réponse Google sans access_token). Avant, l'app stockait des tokens vides et se marquait "connectée" sans rien dire.
• Fix : la reconnexion Google depuis l'app (sans clear data) fonctionne enfin de manière fiable — plus de boucle "popup → rien → retry → rien".
